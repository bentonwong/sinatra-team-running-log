<!DOCTYPE html>
<html>
  <head>
    <title>Your Team Running Log</title>
  </head>

  <body>
    <h1>Your Team Running Log</h1><p>Welcome, <%= @runner.name %>! (<a href='/logout'>logout</a>)</p>
    <h2>Workouts | <button type="button"><a href='/workouts/new'>Enter a new workout</a></h2></button>
    <ul style="list-style-type:none"><% Workout.all.reverse.each do |workout| %>
      <li><p style="font-size:100%;"><%= workout.workout_date.strftime("%a, %b %d, %Y") %> --> <%= Runner.find_by_id(workout.runner_id).name %> ran <a href="/workouts/<%= workout.id %>"><%= workout.distance %></a> miles.</p></li>
      <li><p style="font-size:80%;"><i>Updated at: <%= workout.updated_at.strftime("%c %Z") %></i></p></li>
      <% end %></ul>
  </body>
</html>
