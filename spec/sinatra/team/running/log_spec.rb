require "spec_helper"

RSpec.describe Sinatra::Team::Running::Log do
  it "has a version number" do
    expect(Sinatra::Team::Running::Log::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
